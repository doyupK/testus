package com.testus.testus.repository;

import com.testus.testus.domain.NotificationMapping;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NotificationMappingRepo extends JpaRepository<NotificationMapping, Long> {
}
